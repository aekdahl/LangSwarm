# Content creation pipeline
version: "2.0"
name: "Content Creation Pipeline"

agents:
  - id: "researcher"
    model: "gpt-3.5-turbo"
    system_prompt: "Research the topic thoroughly and gather key information."
    
  - id: "writer"
    model: "gpt-4"
    system_prompt: "Create engaging, well-structured content based on research."
    
  - id: "editor"
    model: "gpt-3.5-turbo"  
    system_prompt: "Polish the content: fix grammar, improve flow, ensure clarity."

workflows:
  # Research → Write → Edit → Deliver
  - "researcher -> writer -> editor -> user"